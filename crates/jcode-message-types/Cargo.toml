[package]
name = "jcode-message-types"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
jcode-base-types = { path = "../jcode-base-types" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
