# Handoff for Conclave Non-Feature Improvements

## Context

You are continuing work on implementing non-feature improvements from the Conclave branch. The goal is to apply all applicable improvements from `conclave_changes.md` while excluding Conclave-specific features.

## Completed Work

The following improvements have been successfully applied:

### 1. Module Consolidation ✅
- Moved `src/tui/markdown/` from `crates/jcode-tui-markdown/`
- Moved `src/tui/mermaid/` from `crates/jcode-tui-mermaid/`
- Moved `src/tui/workspace/` from `crates/jcode-tui-workspace/`
- Updated `Cargo.toml` to remove crate dependencies
- Updated imports throughout the codebase

### 2. Simplified TUI State Management ✅
- Removed `HELP_CMDS_MARKDOWN` constant from `src/tui/app/state_ui.rs`
- Removed `/help-cmds` command handler
- Removed `format_cache_stats` function
- Removed `/cache stats` command handler
- Updated context report to use `env_context_chars` instead of `session_context_chars`

### 3. Simplified Communicate Tool ✅
- Removed `"cleanup"` action and handler from `src/tool/communicate.rs`
- Removed `"report"` action and handler
- Removed `"run_plan"` action and handler
- Removed `"start_task"` alias (merged into `"start"`)
- Made `"status"` action require explicit `target_session`
- Made `"start"`, `"wake"`, `"resume"`, `"retry"`, `"reassign"`, `"replace"`, `"salvage"` require explicit `task_id`
- Removed `working_dir` parameter from all requests and handlers
- Simplified `"await_members"` action to use `format_awaited_members` instead of `format_awaited_members_with_reports`
- Removed helper functions: `cleanup_swarm_workers`, `run_swarm_plan_to_terminal`, `fetch_awaited_member_reports`, `format_awaited_members_with_reports`, `cleanup_candidate_session_ids`, `default_cleanup_target_statuses`
- Updated action lists in schema and error messages

### 4. Cherry-Picked Commit ✅
- Cherry-picked `11cb529b` (docs audit) successfully
- Attempted cherry-picking other commits but they had conflicts due to file deletions/modifications in the current project

## Remaining Work

### Agent 1: Enhanced Selfdev Build Queue Changes

**Task:** Apply enhanced selfdev build queue improvements from conclave_changes.md

**Files to check:**
- `src/tool/selfdev/build_queue.rs`
- `src/tool/selfdev/mod.rs`
- `src/tool/selfdev/reload.rs`
- `src/tool/selfdev/status.rs`
- `src/tool/selfdev/tests.rs`

**Approach:**
1. Read the conclave_changes.md file to understand the specific improvements
2. Check the diff in `conclave_diff.txt` for selfdev-related changes
3. Manually apply the relevant changes to the selfdev files
4. Focus on non-feature improvements (performance, code quality, test coverage)
5. Avoid Conclave-specific features

### Agent 2: Enhanced Swarm Support Changes ✅

**Task:** Apply enhanced swarm support improvements from conclave_changes.md

**Files to check:**
- `src/server/swarm.rs`
- `src/server/swarm_persistence_tests.rs`
- `src/server/comm_control.rs`
- `src/server/comm_control_tests/`
- `src/protocol_tests/comm_requests.rs`
- `src/protocol_tests/comm_responses.rs`

**Approach:**
1. Read the conclave_changes.md file to understand the specific improvements
2. Check the diff in `conclave_diff.txt` for swarm-related changes
3. Manually apply the relevant changes to swarm files
4. Focus on non-feature improvements (performance, code quality, test coverage)
5. Avoid Conclave-specific features

**Completed Changes:**
- Removed legacy swarm task execution functions from `src/server/swarm.rs`:
  - `run_swarm_task` (lines 966-1000)
  - `run_swarm_message` (lines 1002-1067)
  - `SwarmTaskSpec` struct (lines 1069-1075)
  - `parse_swarm_tasks` function (lines 1077-1090)
- Removed test for `parse_swarm_tasks` from swarm.rs test module
- Removed debug commands from `src/server/debug_jobs.rs`:
  - `swarm_message_async:` command handler
- Removed debug command from `src/server/debug_command_exec.rs`:
  - `swarm_message:` command handler
- Updated help text in `src/server/debug_help.rs`:
  - Removed `swarm_message:<text>` and `swarm_message_async:<text>` entries
- Updated `src/server.rs`:
  - Removed `run_swarm_message` from swarm module imports

**Rationale:**
The conclave changes indicated swarm.rs was simplified by +22/-207 lines. The removed functions were older swarm task execution infrastructure only used in debug commands. The modern swarm system uses the communicate tool with plan-based task assignment instead, making these legacy functions unnecessary. This simplification improves code maintainability and reduces complexity.

## Important Notes

1. **Do NOT implement Conclave-specific features** - only apply non-feature improvements
2. The project has significant divergence from conclave (many files deleted, different structure)
3. Manual application is required for most changes due to conflicts
4. Test changes after applying them
5. Update this TODO list as you complete tasks

## Current TODO Status

- ✅ Apply module consolidation
- ✅ Update Cargo.toml to remove crate dependencies
- ✅ Apply simplified TUI state management changes
- ✅ Apply simplified communicate tool changes
- ⏳ Apply enhanced selfdev build queue changes (Agent 1)
- ✅ Apply enhanced swarm support changes (Agent 2)
- ⏳ Apply documentation improvements
- ⏳ Test all applied changes

## Testing

After completing your assigned tasks:
1. Run `cargo build` to ensure the project compiles
2. Run `cargo test` to ensure tests pass
3. Check for any compilation errors or warnings
4. Report any issues found

## Communication

If you encounter issues or need clarification:
1. Check `conclave_changes.md` for context
2. Check the git diff between master and conclave/master for reference
3. Document any blockers or questions
